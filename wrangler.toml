name = "shinya-diary"
compatibility_date = "2024-12-30"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# R2 바인딩
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "shinya-diary"

# Production 환경 변수는 Cloudflare Dashboard에서 설정
# 또는 .dev.vars 파일 사용 (gitignore에 추가됨)
